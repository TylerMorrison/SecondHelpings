import React, { Component } from 'react';

export default class Facts extends Component {
    render() {
        const pageStyle = {
            paddingTop: "50px"
        };

        return (
            <div style={pageStyle}>
                
            </div>
        );
    }
}